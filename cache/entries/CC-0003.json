{
  "$schema": "../../schemas/constraint.schema.json",
  "schema": "decision-archaeology.constraint@v0",
  "id": "CC-0003",
  "type": "REFUTATION",
  "claim": "`git log --follow` is enough to read a record's history across a rename.",
  "tags": [
    "git",
    "rename-detection",
    "history"
  ],
  "environment": {
    "what": "history readers that resolve each revision at the file's current path",
    "versions": [
      "git 2.x"
    ],
    "assumptions": [
      "the move and an edit land in the same commit"
    ]
  },
  "witness": {
    "counterexample": {
      "argv": [
        "python3",
        "tests/admission_gate_test.py"
      ],
      "expected_exit": 0,
      "means": "the suite builds the counterexample scenario and shows the sound check rejecting it; it is not a separated pair, so this entry may not block"
    }
  },
  "applies_until": "Each revision is read at the name the file carried then, and a moved record declares `moved_from`. Note rename detection is a similarity heuristic: a move that edits enough of the file reports as add+delete, not as a rename.",
  "cost": {
    "to_discover": "one adversarial review round",
    "to_recheck": "seconds"
  },
  "authority": "informs-only"
}
