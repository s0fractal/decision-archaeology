[project]
name = "decision-archaeology-toolchain"
version = "0.0.0"
description = "Locked published verification tools for Decision Archaeology cases"
requires-python = "==3.13.*"
dependencies = [
    "sigma-glyph==0.6.7",
    "warrant-verify==0.9.0",
]

[tool.uv]
package = false
